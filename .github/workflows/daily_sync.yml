name: 🏆 2026 FIFA Daily Data Sync

on:
  schedule:
    # GitHub cron is best-effort and is frequently delayed by hours under load
    # (a single 07:00 run was landing anywhere from 09:00–17:00 UTC). To reliably
    # have results on the site by 15:00 Taiwan (07:00 UTC), fire several times in
    # the morning window so at least one lands by the deadline despite delays,
    # plus afternoon/evening runs to catch same-day late (Taiwan-evening) matches.
    - cron: '0 1 * * *'    # 09:00 Taiwan
    - cron: '0 3 * * *'    # 11:00 Taiwan
    - cron: '0 5 * * *'    # 13:00 Taiwan
    - cron: '0 6 * * *'    # 14:00 Taiwan
    - cron: '0 7 * * *'    # 15:00 Taiwan  ← deadline
    - cron: '0 11 * * *'   # 19:00 Taiwan
    - cron: '0 15 * * *'   # 23:00 Taiwan
  push:
    # Any push to main also refreshes data immediately (the sync's own commit uses
    # [skip ci], so this does not loop). Lets a merge trigger an on-demand update.
    branches: [main]
  workflow_dispatch: # Allows manual execution from GitHub actions tab

# Never let two syncs run at once (they both commit to main and would race).
concurrency:
  group: daily-sync
  cancel-in-progress: false

permissions:
  contents: write # Crucial permission to allow push back to repository

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: 🔍 Checkout Repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: 📦 Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4

      - name: ⚡ Run Quantitative Sync & Prediction Engine
        env:
          THE_ODDS_API_KEY: ${{ secrets.THE_ODDS_API_KEY }}
        run: |
          python sync_fifa.py

      - name: Sync External Prediction Sources
        run: |
          python external_predictions.py

      - name: 🎟️ Settle Personal Bets & Refresh Win-Rate Report
        run: |
          python bet_tracker.py

      - name: 📡 Evaluate Live Prediction Accuracy
        run: |
          python prediction_eval.py

      - name: 🏗️ Build Static Site (SEO/AdSense-ready, for GitHub Pages)
        run: |
          python build_static.py

      - name: 💾 Git Commit and Push Updates
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          # Check if there are changes to commit
          if [ -n "$(git status --porcelain)" ]; then
            git add fifa_2026.db sync.log CHAMPION_TREND.md BET_TRACKING.md PRED_ACCURACY.md prediction_metrics_live.json docs
            git commit -m "chore: daily data, odds sync & static site rebuild [skip ci]"
            git push origin HEAD:${{ github.ref }}
          else
            echo "No database or log changes to commit."
          fi
